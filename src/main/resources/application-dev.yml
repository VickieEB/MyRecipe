spring:
  datasource:
    url: jdbc:mysql://localhost:3306/pp_dev
    username: pp_dev_user
    password: petproject
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: true
#    properties:
#      javax:
#        persistence:
#          schema-generation:
#            create-source: metadata
#            scripts:
#              action: create
#              create-target: pp_database_create.sql

